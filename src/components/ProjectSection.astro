---
// Componente ProjectSection unificado
---

<!-- Sección Proyecto -->
<section id="proyecto" class="section project-hero">
  <div class="container">
    <div class="project-grid">
      <div class="project-content">
        <div class="intro-header fade-in" data-delay="0.1">
          <span class="section-tag">El Proyecto</span>
          <h2>Patrimonio y modernidad en perfecta armonía</h2>
        </div>
        <p class="fade-in" data-delay="0.2">En el centro de Santoña (Cantabria), junto al Ayuntamiento y la histórica Plaza de San Antonio, recuperamos un edificio histórico para crear ocho viviendas únicas y dos locales comerciales. Un proyecto que combina la riqueza patrimonial con las más altas calidades constructivas contemporáneas.</p>
      </div>
      <div class="project-image fade-in" data-delay="0.3">
        <div class="image-container">
          <img 
            src="/images/fachada.jpg" 
            alt="Fachada completa del edificio histórico ARO 3 en Santoña"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Sección de Diseño -->
<section class="section design-section">
  <div class="design-wrapper">
    <div class="design-image">
      <img 
        src="/images/buhardilla.jpg" 
        alt="Diseño de interiores de lujo en Residencial ARO 3"
        class="fade-in"
        data-delay="0.4"
        loading="lazy"
      />
    </div>
    <div class="design-content fade-in" data-delay="0.5">
      <h2>Diseño que perdura, espacios que inspiran.</h2>
      <p>Cada estancia ha sido pensada para transmitir equilibrio y calidez. La luz natural se funde con los acabados de alta gama, creando una atmósfera acogedora. Con líneas modernas, amplitud, altura y materiales cuidadosamente elegidos, este piso es el lugar ideal para disfrutar de Santoña con estilo propio y gusto por los detalles.</p>
    </div>
  </div>
</section>

<style>
  /* Estilos comunes */
  .section {
    padding: 8rem 0;
    background: var(--white);
    overflow: hidden;
  }

  .container {
    width: 100%;
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Estilos para la sección de proyecto */
  .project-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
    margin: 0;
    padding: 0;
  }

  .project-content {
    max-width: 600px;
    padding-right: 40px;
  }

  .project-image {
    width: 100%;
    margin: 0;
    padding: 0;
    opacity: 0;
    transform: translateX(30px);
    transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1), 
                transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .project-image.visible {
    opacity: 1;
    transform: translateX(0);
  }

  .project-image img {
    width: 100%;
    max-height: 80vh;
    object-fit: contain;
    object-position: center;
    transition: transform 1.2s cubic-bezier(0.2, 0.6, 0.2, 1);
    will-change: transform;
  }

  .project-image:hover img {
    transform: scale(1.02);
  }

  .image-container {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Estilos para la sección de diseño (fullscreen con texto superpuesto mejorado) */
  .design-section {
    position: relative;
    padding: 0;
    height: 100vh;
    overflow: hidden;
    background: black;
  }

  .design-wrapper {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .design-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .design-image::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.35); /* overlay oscuro semitransparente */
    z-index: 2;
  }

  .design-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transition: transform 1.2s cubic-bezier(0.2, 0.6, 0.2, 1);
    z-index: 1;
  }

  .design-image:hover img {
    transform: scale(1.02);
  }

  .design-content {
    position: absolute;
    top: 50%;
    right: 15%; /* más a la izquierda */
    transform: translateY(-50%);
    z-index: 3; /* sobre el overlay */
    max-width: 600px;
    color: white;
    text-shadow: 0 2px 10px rgba(0,0,0,0.8); /* sombra más fuerte */
  }

  .design-content h2,
  .design-content p {
    color: white;
  }

  /* Estilos de tipografía */
  .intro-header {
    margin-bottom: 25px;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  .intro-header.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .section-tag {
    display: block;
    font-size: 1.3rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--primary);
    margin-bottom: 15px;
    position: relative;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.8s ease 0.1s;
  }

  .section-tag.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .section-tag::after {
    content: '';
    position: absolute;
    bottom: -12px;
    left: 0;
    width: 50px;
    height: 2px;
    background: var(--primary);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.8s ease 0.2s;
  }

  .section-tag.visible::after {
    transform: scaleX(1);
  }

  h2 {
    font-size: 3.8rem;
    font-weight: 300;
    color: var(--dark);
    margin: 0 0 1.5rem 0;
    line-height: 1.2;
    letter-spacing: -1px;
    font-family: var(--font-serif);
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.8s ease 0.2s;
  }

  h2.visible {
    opacity: 1;
    transform: translateY(0);
  }

  p {
    font-size: 1.8rem;
    line-height: 1.8;
    color: var(--text-secondary);
    font-weight: 300;
    margin-bottom: 0;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.8s ease 0.3s;
  }

  p.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Estilos responsivos */
  @media (max-width: 1024px) {
    .project-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .project-content {
      padding-right: 0;
      max-width: 100%;
    }

    .design-content {
      right: 50%;
      transform: translate(50%, -50%);
      text-align: center;
      max-width: 90%;
    }
  }

  @media (max-width: 768px) {
    h2 {
      font-size: 3.2rem;
    }

    p {
      font-size: 1.7rem;
    }

    .section {
      padding: 6rem 0;
    }
  }

  @media (max-width: 480px) {
    h2 {
      font-size: 2.8rem;
    }

    p {
      font-size: 1.6rem;
    }
  }
</style>

<script>
  // Intersection Observer para animaciones al hacer scroll
  document.addEventListener('DOMContentLoaded', () => {
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const animateOnScroll = (entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          if (entry.target.classList.contains('fade-in')) {
            const delay = entry.target.getAttribute('data-delay') || '0';
            setTimeout(() => {
              entry.target.style.opacity = '1';
              entry.target.style.transform = 'translateY(0)';
            }, parseFloat(delay) * 1000);
          } else {
            entry.target.classList.add('visible');
          }
          observer.unobserve(entry.target);
        }
      });
    };

    const observer = new IntersectionObserver(animateOnScroll, observerOptions);

    // Observar elementos con clase fade-in
    document.querySelectorAll('.fade-in').forEach(element => {
      element.style.opacity = '0';
      element.style.transform = 'translateY(20px)';
      element.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
      observer.observe(element);
    });

    // Observar elementos específicos para animar
    const elementsToAnimate = [
      ...document.querySelectorAll('.project-image, .intro-header, .section-tag, h2, p, .design-content')
    ];

    elementsToAnimate.forEach(element => {
      if (element) observer.observe(element);
    });
  });
</script>

