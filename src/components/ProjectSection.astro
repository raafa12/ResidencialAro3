---
// src/components/ProjectSection.astro
---

<section id="proyecto" class="section project-hero">
  <div class="container">
    <div class="project-grid">
      <div class="project-content fade-in">
        <div class="intro-header">
          <span class="section-tag">El Proyecto</span>
          <h2>Patrimonio y modernidad en perfecta armonía</h2>
        </div>
        <p>En el centro de Santoña (Cantabria), junto al Ayuntamiento y la histórica Plaza de San Antonio, recuperamos un edificio histórico para crear ocho viviendas únicas y dos locales comerciales. Un proyecto que combina la riqueza patrimonial con las más altas calidades constructivas contemporáneas.</p>
      </div>
      <div class="project-image">
        <div class="image-container">
          <img 
            src="/images/fachada.jpg" 
            alt="Fachada completa del edificio histórico ARO 3 en Santoña"
            class="fade-in"
            data-delay="0.2"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<section id="caracteristicas" class="features-showcase">
  <div class="features-image">
    <img 
      src="/images/interiorr.jpg" 
      alt="Interior elegante y moderno del edificio ARO 3"
      loading="lazy"
    />
    <div class="features-overlay">
      <div class="features-content">
        <h2 class="fade-in">Diseño que perdura, espacios que inspiran.</h2>
        <p class="fade-in" data-delay="0.1">
          Cada estancia ha sido pensada para transmitir equilibrio y calidez. La luz natural se funde con los acabados de alta gama, creando una atmósfera acogedora. Con líneas modernas, amplitud, altura y materiales cuidadosamente elegidos, este piso es el lugar ideal para disfrutar de Santoña (Cantabria) con estilo propio y gusto por los detalles.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .project-hero {
    padding: 8rem 0;
    background: var(--white);
    overflow: hidden;
  }

  .project-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
    box-shadow: none !important;
  }

  .project-content {
    max-width: 600px;
    padding-right: 40px;
  }

  .project-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 50% 0%; /* enfoca arriba manteniendo el tamaño */
  /* Si necesitas subirla aún más, descomenta la línea de abajo: */
  /* transform: translateY(-10%); */
  box-shadow: none !important;
  transition: transform 1.2s cubic-bezier(0.2, 0.6, 0.2, 1);
  will-change: transform;
  display: block;
}

  .image-container {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    border-radius: 8px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15) !important;
    transform: translateZ(0);
  }

  .project-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    box-shadow: none !important;
    transition: transform 1.2s cubic-bezier(0.2, 0.6, 0.2, 1);
    will-change: transform;
    display: block;
  }

  .project-image:hover img {
    transform: scale(1.03);
  }

  .project-image::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: none !important;
    z-index: 1;
    pointer-events: none;
  }

  @media (max-width: 1024px) {
    .project-grid {
      grid-template-columns: 1fr;
      gap: 50px;
    }

    .project-content {
      padding-right: 0;
      max-width: 100%;
    }

    .project-image {
      margin-top: 0;
      max-height: 600px;
    }
  }

  @media (max-width: 768px) {
    .project-hero {
      padding: 6rem 0;
    }

    .project-grid {
      gap: 40px;
    }

    .project-image {
      max-height: 500px;
    }
  }

  .intro-header {
    margin-bottom: 40px;
  }

  .section-tag {
    display: block;
    font-size: 1.3rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--primary);
    margin-bottom: 25px;
    position: relative;
  }

  .section-tag::after {
    content: '';
    position: absolute;
    bottom: -12px;
    left: 0;
    width: 50px;
    height: 2px;
    background: var(--primary);
  }

  .project-content h2 {
    font-size: 3.8rem;
    font-weight: 300;
    color: var(--dark);
    margin: 0 0 1.5rem 0;
    line-height: 1.2;
    letter-spacing: -1px;
  }

  .project-content p {
    font-size: 1.8rem;
    line-height: 1.8;
    color: var(--text-secondary);
    font-weight: 300;
    margin-bottom: 0;
  }

  .features-showcase {
    position: relative;
    width: 100%;
    overflow: hidden;
    margin: 0;
    padding: 0;
  }

  .features-image {
    position: relative;
    width: 100%;
    height: 85vh;
    min-height: 600px;
    max-height: 800px;
  }

  .features-image::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.35);
    z-index: 1;
  }

  .features-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    position: relative;
    display: block;
  }

  .features-overlay {
    position: absolute;
    top: 0;
    right: 0;
    left: auto;
    width: 50%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    background: none;
    padding: 0 5%;
    color: white;
    z-index: 2;
  }

  .features-content {
    max-width: 500px;
    margin-left: 0;
    padding: 0 20px;
  }

  .features-content h2 {
    font-size: 3.4rem;
    font-weight: 400;
    line-height: 1.2;
    margin-bottom: 2rem;
    color: white;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
  }

  .features-content p {
    font-size: 1.8rem;
    line-height: 1.7;
    margin-bottom: 0;
    color: rgba(255, 255, 255, 0.95);
    text-shadow: 0 1px 4px rgba(0, 0, 0, 0.3);
  }

  @media (max-width: 1024px) {
    .features-image {
      height: 80vh;
      min-height: 550px;
    }

    .features-overlay {
      width: 65%;
      padding: 0 4%;
    }
    
    .features-content {
      max-width: 100%;
      padding: 0 20px;
    }

    .features-content h2 {
      font-size: 3rem;
    }

    .features-content p {
      font-size: 1.7rem;
    }
  }

  @media (max-width: 768px) {
    .features-showcase {
      position: relative;
      min-height: 100vh;
    }

    .features-image {
      position: relative;
      width: 100%;
      height: 100vh;
      min-height: 100vh;
      max-height: none;
    }

    .features-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .features-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 5%;
      box-sizing: border-box;
    }

    .features-content {
      max-width: 600px;
      width: 100%;
      color: white;
      text-align: center;
      padding: 0 20px;
    }

    .features-content h2 {
      font-size: 2.6rem;
      margin-bottom: 1.8rem;
      line-height: 1.3;
      color: white;
      text-align: center;
    }

    .features-content p {
      font-size: 1.6rem;
      line-height: 1.7;
      margin-bottom: 0;
      color: rgba(255, 255, 255, 0.95);
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .project-content h2 {
      font-size: 3.2rem;
    }

    .project-content p {
      font-size: 1.7rem;
    }

    .features-content h2 {
      font-size: 2.2rem;
    }
    
    .features-content p {
      font-size: 1.5rem;
    }
  }

  .fade-in {
    opacity: 0;
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .fade-in.in-view {
    opacity: 1;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const fadeElements = document.querySelectorAll('.fade-in');
    
    const fadeInOnScroll = () => {
      fadeElements.forEach(element => {
        const elementTop = element.getBoundingClientRect().top;
        const elementVisible = 150;
        const delay = element.dataset.delay || 0;
        
        if (elementTop < window.innerHeight - elementVisible) {
          setTimeout(() => {
            element.classList.add('in-view');
          }, delay * 1000);
        }
      });
    };

    // Initial check
    fadeInOnScroll();
    
    // Check on scroll
    window.addEventListener('scroll', fadeInOnScroll);
  });
</script>