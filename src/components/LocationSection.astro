---
// src/components/LocationSection.astro
---

<section id="calidades" class="section-narrow specifications">
  <div class="container">
    <div class="spec-header fade-in">
      <span class="section-tag">Calidades Premium</span>
      <h2>Selección cuidadosa de materiales</h2>
      <p>Acabados de las mejores marcas para garantizar durabilidad, elegancia y confort en cada detalle</p>
    </div>
    <div class="specs-grid">
      <div class="spec-category fade-in" data-delay="0.1">
        <div class="category-header">
          <div class="category-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <rect x="3" y="4" width="18" height="16" rx="2" stroke="currentColor" stroke-width="1.5"/>
              <path d="M7 8h10M7 12h6M7 16h8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
          </div>
          <h3>Equipamiento</h3>
        </div>
        <div class="spec-list">
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Electrodomésticos</span>
              <span class="spec-value">Bosch integrados<br><small>Horno, vitrocerámica, lavavajillas, frigorífico</small></span>
            </div>
          </div>
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Sanitarios</span>
              <span class="spec-value">Jacob Delafon<br><small>Villeroy & Boch</small></span>
            </div>
          </div>
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Grifería</span>
              <span class="spec-value">Hansgrohe</span>
            </div>
          </div>
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Ascensor</span>
              <span class="spec-value">Orona</span>
            </div>
          </div>
        </div>
      </div>
      
      <div class="spec-category fade-in" data-delay="0.2">
        <div class="category-header">
          <div class="category-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M3 7V17a2 2 0 002 2h14a2 2 0 002-2V7M3 7l9 6 9-6M3 7l2-4h14l2 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3>Acabados</h3>
        </div>
        <div class="spec-list">
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Pavimentos</span>
              <span class="spec-value">Laminado flotante<br><small>Maderas nobles y gres porcelánico</small></span>
            </div>
          </div>
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Carpintería exterior</span>
              <span class="spec-value">Madera recuperada<br><small>Original con aluminio en patio</small></span>
            </div>
          </div>
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Carpintería interior</span>
              <span class="spec-value">Madera lacada blanco<br><small>Frentes de armario incluidos</small></span>
            </div>
          </div>
          <div class="spec-item">
            <div class="spec-content">
              <span class="spec-label">Aislamiento</span>
              <span class="spec-value">Térmico y acústico<br><small>Lana mineral 45mm</small></span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>



<style>
  .specifications {
    background: var(--white);
    border-top: 1px solid var(--border);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .spec-header {
    text-align: center;
    margin-bottom: 80px;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .section-tag {
    display: inline-block;
    font-size: 1.1rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--primary);
    margin-bottom: 15px;
    position: relative;
    text-align: center;
    width: 100%;
  }

  .section-tag::after {
    content: '';
    position: absolute;
    bottom: -12px;
    left: 50%;
    transform: translateX(-50%);
    width: 50px;
    height: 2px;
    background: var(--primary);
  }

  .spec-header h2 {
    font-size: 3.5rem;
    font-weight: 300;
    color: var(--dark);
    margin: 10px 0 1.5rem 0;
    line-height: 1.2;
    letter-spacing: -1px;
  }

  .spec-header p {
    font-size: 1.1rem;
    line-height: 1.8;
    color: var(--gray-600);
    font-weight: 300;
    max-width: 700px;
    margin: 0 auto;
  }

  .specs-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
  }

  .spec-category {
    background: var(--white);
    border-radius: 12px;
    padding: 40px 30px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    border: 1px solid var(--border);
    transition: all 0.4s ease;
    opacity: 0;
    transform: translateY(30px);
  }

  .spec-category.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  .spec-category:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 30px rgba(0, 103, 144, 0.1);
  }

  .category-header {
    display: flex;
    align-items: center;
    margin-bottom: 30px;
    padding-bottom: 20px;
    border-bottom: 1px solid var(--border);
  }

  .category-icon {
    width: 50px;
    height: 50px;
    background: var(--light);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--primary);
    margin-right: 15px;
    transition: all 0.3s ease;
  }

  .spec-category:hover .category-icon {
    background: var(--primary);
    color: var(--white);
  }

  .category-header h3 {
    font-size: 1.4rem;
    font-weight: 400;
    color: var(--dark);
    margin: 0;
    letter-spacing: 0.5px;
  }

  .spec-list {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .spec-item {
    padding: 20px 0;
    border-bottom: 1px solid rgba(232, 232, 232, 0.5);
    transition: all 0.3s ease;
  }

  .spec-item:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .spec-item:hover {
    padding-left: 10px;
  }

  .spec-content {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .spec-label {
    font-weight: 500;
    color: var(--text-primary);
    font-size: 0.95rem;
    flex: 1;
    min-width: 120px;
  }

  .spec-value {
    font-weight: 300;
    color: var(--text-secondary);
    text-align: right;
    font-size: 0.9rem;
    flex: 1;
    line-height: 1.5;
  }

  .spec-value small {
    display: block;
    font-size: 0.8rem;
    color: var(--text-light);
    margin-top: 2px;
  }

  /* Location Section */
  .location {
    background: var(--dark);
    color: var(--white);
    padding: 120px 0;
    position: relative;
    overflow: hidden;
  }

  .location::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, var(--primary-darker) 0%, var(--primary-dark) 100%);
    opacity: 0.9;
  }

  .location-content {
    position: relative;
    z-index: 2;
    text-align: center;
    margin-bottom: 60px;
  }

  .location-header h2 {
    font-size: 3.5rem;
    font-weight: 300;
    margin-bottom: 3rem;
    letter-spacing: -1px;
    color: var(--white);
  }

  .location-text {
    max-width: 800px;
    margin: 0 auto;
  }

  .location-text p {
    font-size: 1.15rem;
    line-height: 1.8;
    opacity: 0.95;
    font-weight: 300;
    margin-bottom: 1.5rem;
    color: var(--white);
  }

  .location-text p:last-child {
    margin-bottom: 0;
  }

  .location-highlights {
    display: flex;
    justify-content: center;
    gap: 60px;
    position: relative;
    z-index: 2;
  }

  .highlight {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    opacity: 0.8;
    transition: all 0.3s ease;
  }

  .highlight:hover {
    opacity: 1;
    transform: translateY(-2px);
  }

  .highlight-icon {
    width: 50px;
    height: 50px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--white);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
  }

  .highlight:hover .highlight-icon {
    background: rgba(255, 255, 255, 0.2);
    transform: scale(1.1);
  }

  .highlight span {
    font-size: 0.9rem;
    font-weight: 400;
    color: var(--white);
    text-align: center;
  }

  /* Animations */
  .fade-in {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .fade-in.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  .fade-in[data-delay="0.1"] { transition-delay: 0.1s; }
  .fade-in[data-delay="0.2"] { transition-delay: 0.2s; }
  .fade-in[data-delay="0.3"] { transition-delay: 0.3s; }

  @media (max-width: 1024px) {
    .specs-grid {
      gap: 60px;
    }
    
    .spec-category {
      padding: 35px 25px;
    }
    
    .location {
      padding: 100px 0;
    }
    
    .location-highlights {
      gap: 40px;
    }
  }

  @media (max-width: 768px) {
    .specs-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    
    .spec-category {
      padding: 30px 20px;
    }
    
    .spec-header h3 {
      font-size: 1.6rem;
    }
    
    .location-header h2 {
      font-size: 2.8rem;
      margin-bottom: 2rem;
    }
    
    .location-text p {
      font-size: 1rem;
    }
    
    .location-highlights {
      flex-direction: column;
      gap: 30px;
    }
  }

  @media (max-width: 480px) {
    .spec-content {
      flex-direction: column;
      gap: 8px;
    }
    
    .spec-value {
      text-align: left;
    }
    
    .category-header {
      flex-direction: column;
      text-align: center;
      gap: 10px;
    }
    
    .category-icon {
      margin-right: 0;
    }
    
    .location-header h2 {
      font-size: 2.4rem;
    }
  }
</style>
